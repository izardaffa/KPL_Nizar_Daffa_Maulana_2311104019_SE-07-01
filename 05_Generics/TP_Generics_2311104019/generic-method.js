class HaloGeneric {
    // Method generik untuk menyapa user
    SapaUser(user) {
        console.log(`Halo user ${user}`);
    }
}

// Contoh penggunaan
const halo = new HaloGeneric();
halo.SapaUser("Nizar");